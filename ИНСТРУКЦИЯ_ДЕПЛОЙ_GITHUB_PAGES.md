# 🚀 Инструкция по деплою проекта "Карта Апрашки" на GitHub Pages

## 📍 Обзор

Это пошаговая инструкция по публикации проекта "Карта Апрашки" на GitHub Pages. После следования этой инструкции проект будет доступен по URL: `https://username.github.io/apraxin/apra-shka-map/`

---

## ✅ Предварительные требования

- ✅ Git установлен на машине
- ✅ GitHub аккаунт создан (https://github.com)
- ✅ Репозиторий "apraxin" создан на GitHub
- ✅ Все файлы проекта готовы в папке `apra-shka-map/`
- ✅ Все <meta> теги с ключами присутствуют в HTML
- ✅ .gitignore содержит защиту для .env и config.js

---

## 🔧 Шаг 1: Подготовка локального репозитория

### 1.1 Если репозиторий еще не инициализирован

Откройте терминал в корне проекта:

```bash
cd /workspaces/apraxin
```

### 1.2 Инициализируем Git (если не сделано)

```bash
git init
```

### 1.3 Добавляем все файлы в Git

```bash
git add .
```

### 1.4 Проверяем что будет добавлено (опционально)

```bash
git status
```

**Вы должны увидеть:**
- ✅ Все .html файлы (index.html, admin.html, pavilion.html, superadmin.html)
- ✅ Все .js файлы (auth.js, data.js, map.js, и т.д.)
- ✅ Все .css файлы (style.css, admin.css)
- ✅ Документацию (README.md, SUPABASE_KEYS.md, и т.д.)
- ❌ НЕ должны быть: .env, js/config.js, node_modules/ (защищены .gitignore)

### 1.5 Делаем первый коммит

```bash
git commit -m "Initial commit: Карта Апрашки MVP с безопасной конфигурацией Supabase"
```

**Рекомендуемое сообщение коммита:**
```
Initial commit: Карта Апрашки MVP

- Интерактивная карта Апраксина двора
- Система аутентификации по номеру телефона
- Админ-панель для управления павильонами
- Супер-админка для управления проектом
- Безопасная конфигурация Supabase (через <meta> теги)
- Row-Level Security в Supabase
- Progressive Web App (PWA) поддержка
```

### 1.6 Настраиваем Git конфиг (если не сделано ранее)

```bash
git config --global user.email "your-email@example.com"
git config --global user.name "Your Name"
```

---

## 🌐 Шаг 2: Подключение к GitHub репозиторию

### 2.1 Добавляем remote (если не сделано)

```bash
git remote add origin https://github.com/YOUR-USERNAME/apraxin.git
```

**Замените:**
- `YOUR-USERNAME` на ваше GitHub имя пользователя
- Например: `git remote add origin https://github.com/Paganets/apraxin.git`

### 2.2 Проверяем что remote добавлен

```bash
git remote -v
```

**Вывод должен быть:**
```
origin  https://github.com/YOUR-USERNAME/apraxin.git (fetch)
origin  https://github.com/YOUR-USERNAME/apraxin.git (push)
```

### 2.3 Отправляем код на GitHub

```bash
git push -u origin main
```

**Если у вас ветка называется "master":**
```bash
git push -u origin master
```

**Первая загрузка может потребовать аутентификацию:**
- Введите ваш GitHub username
- Введите Personal Access Token (PAT) вместо пароля

**Как создать PAT:**
1. GitHub → Settings → Developer settings → Personal access tokens
2. Generate new token (classic)
3. Выберите scopes: `repo` (полный доступ к репозиториям)
4. Скопируйте токен и используйте его как пароль при git push

---

## ⚙️ Шаг 3: Настройка GitHub Pages

### 3.1 Откройте репозиторий на GitHub

Перейдите на: https://github.com/YOUR-USERNAME/apraxin

### 3.2 Откройте вкладку "Settings"

- Нажмите кнопку **Settings** (в топ-баре репозитория)

### 3.3 Перейдите в "Pages"

- На левой панели найдите **Pages** (или **Code and automation** → **Pages**)
- Нажмите на **Pages**

### 3.4 Настройте GitHub Pages

**Build and deployment:**
- **Source:** Deploy from a branch
- **Branch:** Выберите `main` (или `master` если вы используете эту ветку)
- **Folder:** Выберите `/ (root)` 

⚠️ **ВАЖНО:** Выберите **`root`** для простоты! Если нужна папка, выберите `/apra-shka-map`

### 3.5 Нажмите "Save"

Откроется уведомление:
```
GitHub Pages is currently disabled. Is your site published at https://yourname.github.io/apraxin/?
```

---

## 🔄 Шаг 4: Ждём деплоя

### 4.1 Правильная конфигурация (source = root)

Если выбрали **root** как source folder:
- Проект должен быть в папке `/apra-shka-map/` внутри репозитория
- GitHub Pages будет искать `index.html` в корне репозитория ❌ Это НЕПРАВИЛЬНО!

**ПРАВИЛЬНЫЙ СПОСОБ:**

Если ваша структура:
```
apraxin/
  └── apra-shka-map/
      ├── index.html
      ├── admin.html
      ├── css/
      ├── js/
      └── ...
```

То вам нужно **выбрать папку `/apra-shka-map`** в GitHub Pages, или перейти на:
```
https://username.github.io/apraxin/apra-shka-map/
```

### 4.2 Процесс деплоя

1. **Перейдите в "Actions"** на странице репозитория
2. Вы должны увидеть workflow с названием "pages build and deployment"
3. **Статус:**
   - 🔵 **Pending** → Очередь
   - 🟡 **In progress** → Идёт деплой
   - 🟢 **Completed** → Готово!

### 4.3 Сколько ждать?

- Обычно: **30 секунд - 2 минуты**
- Максимум: **5 минут**
- Если больше 10 минут - проверьте вкладку "Actions" на ошибки

---

## ✅ Шаг 5: Проверка деплоя

### 5.1 Получить ссылку на проект

После успешного деплоя вы увидите сообщение:
```
Your site is live at https://username.github.io/apraxin/apra-shka-map
```

**Замените `username` на ваше GitHub имя:**
- Например: https://github.com/Paganets/apraxin → https://Paganets.github.io/apraxin/apra-shka-map

### 5.2 Откройте ссылку в браузере

```
https://username.github.io/apraxin/apra-shka-map/
```

### 5.3 Проверьте что работает

✅ Загрузилась ли главная страница?
✅ Видна ли карта Апраксина двора?
✅ Работает ли поиск и фильтры?
✅ Можно ли войти по номеру телефона (+79602406033)?
✅ Загружаются ли CSS стили (или всё белое)?

### 5.4 Если всё белое или 404

**Решение 1: Проверьте путь к файлам**
- Откройте F12 (консоль браузера)
- Посмотрите вкладку "Network"
- Проверьте что все файлы загружаются (нет ошибок 404)

**Решение 2: Проверьте структуру папок**
- Убедитесь что `index.html` находится в папке `/apra-shka-map/`
- GitHub Pages ищет файлы в выбранной папке

**Решение 3: Очистите кэш браузера**
```
Ctrl+Shift+Delete (или Cmd+Shift+Delete на Mac)
```
Удалите кэш и зайдите заново

---

## 🔒 Шаг 6: Проверка безопасности перед деплоем

### ✅ Убедитесь перед git push:

1. **Проверьте что ключи НЕ в JS файлах:**
   ```bash
   grep -r "SUPABASE_ANON_KEY\|SUPABASE_URL" apra-shka-map/js/
   ```
   **Результат должен быть пуст** (кроме инициализации из window.SupabaseConfig)

2. **Проверьте что <meta> теги есть в HTML:**
   ```bash
   grep "supabase-url\|supabase-anon-key" apra-shka-map/*.html
   ```
   **Результат должен показать 4 строки** (по 2 на каждый .html файл)

3. **Проверьте что .env не коммитится:**
   ```bash
   git log --all --full-history -p -- .env | head -20
   ```
   **Результат должен быть пуст** (файл не должен быть в истории)

4. **.gitignore содержит правильные исключения:**
   ```bash
   cat apra-shka-map/.gitignore | grep -E "\.env|config\.js"
   ```
   **Результат должен содержать .env и js/config.js**

---

## 📚 Дополнительные команды

### Обновить проект после изменений

После того как вы что-то изменили:

```bash
# 1. Добавляем изменения
git add .

# 2. Коммитим
git commit -m "Описание изменений"

# 3. Отправляем на GitHub
git push origin main
```

GitHub Pages автоматически перестроится через 30 секунд - 2 минуты!

### Посмотреть историю коммитов

```bash
git log --oneline
```

### Откатить последний коммит (если ошибка)

```bash
git reset --soft HEAD~1
```

### Удалить локальный репозиторий (начать заново)

```bash
rm -rf .git
```

---

## 🐛 Решение проблем при деплое

### Проблема: "GitHub Pages is not enabled"

**Решение:**
1. Откройте Settings → Pages
2. Убедитесь что Source выбран (не "None")
3. Выберите ветку `main` и папку `apra-shka-map` (или `/`)
4. Нажмите Save

### Проблема: "Error in workflow - permission denied"

**Решение:**
1. Откройте Settings → Actions
2. Найдите "Workflow permissions"
3. Выберите "Read and write permissions"
4. Сохраните и повторите попытку

### Проблема: "404 Not Found"

**Решение:**
1. Проверьте что структура папок правильная
2. Убедитесь что index.html находится в нужной порке
3. Проверьте браузер консоль (F12) на ошибки
4. Попробуйте пересоздать Pages (отключить и включить)

### Проблема: "Только главная страница работает, остальные - 404"

**Решение:**
Это нормально для single-page приложений! Все ссылки должны вести на index.html.
Проверьте что в вашем JavaScript код обрабатывает маршруты правильно.

---

## 📋 Чек-лист перед деплоем

- [ ] Все файлы добавлены в Git (`git status` пуст)
- [ ] Коммит сделан с понятным сообщением
- [ ] Репозиторий подключен к GitHub (`git remote -v` показывает origin)
- [ ] Код залит на GitHub (`git push` выполнен)
- [ ] GitHub Pages включена в Settings → Pages
- [ ] Выбрана правильная ветка (main) и папка (apra-shka-map или root)
- [ ] .env файл НЕ в Git (проверено .gitignore)
- [ ] js/config.js НЕ в Git (проверено .gitignore)
- [ ] <meta> теги с Supabase ключами есть в HTML
- [ ] Деплой завершён (Actions показывает зелёный статус)
- [ ] Проект доступен по ссылке https://username.github.io/apraxin/apra-shka-map/
- [ ] Главная страница загружается (видна карта)
- [ ] CSS стили применяются (не белая пустая страница)

---

## 🎉 Поздравляем!

Ваш проект "Карта Апрашки" успешно развёрнут на GitHub Pages!

### Что дальше?

1. **Поделитесь ссылкой:**
   ```
   https://username.github.io/apraxin/apra-shka-map/
   ```

2. **Добавьте ссылку в README.md:**
   ```markdown
   ## 🌐 Демо
   
   Проект доступен на GitHub Pages:
   [Карта Апрашки](https://username.github.io/apraxin/apra-shka-map/)
   ```

3. **Продолжайте разработку:**
   - Вносите изменения локально
   - Коммитьте с `git commit`
   - Отправляйте с `git push`
   - GitHub Pages автоматически обновится!

4. **Мониторьте деплой:**
   - Откройте вкладку "Actions" в репозитории
   - Следите за статусом workflows
   - Если ошибка - нажмите на workflow чтобы увидеть логи

---

## 📞 Справка

**Официальная документация:**
- GitHub Pages: https://pages.github.com/
- GitHub Docs: https://docs.github.com/pages
- Supabase Security: https://supabase.com/docs/guides/auth

**Проект на GitHub:**
- Репозиторий: https://github.com/username/apraxin
- Демо: https://username.github.io/apraxin/apra-shka-map/

---

**Успешного деплоя! 🚀**
