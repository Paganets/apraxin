# 🔐 Инструкция по отладке аутентификации и Supabase

## 📝 Оглавление
1. [Проверка данных в Supabase](#1-проверка-данных-в-supabase)
2. [Как включить логирование](#2-как-включить-логирование)
3. [Понимание логов](#3-понимание-логов)
4. [Частые ошибки и решения](#4-частые-ошибки-и-решения)
5. [Тестирование](#5-тестирование)

---

## 1. Проверка данных в Supabase

### Шаг 1: Откройте консоль Supabase
1. Перейдите на https://supabase.com
2. Откройте ваш проект "apraxin"
3. Слева нажмите **"SQL Editor"**

### Шаг 2: Проверьте наличие номера в таблице
Выполните этот SQL запрос:
```sql
SELECT id, name, phone, approved, email, created_at 
FROM public.tenants 
WHERE phone = '+79602406033' 
LIMIT 10;
```

**Что проверяем:**
- ✅ **id** - должен быть UUID (например: `ec66395d-a6a1-4d83-8dad-322382dfe9b9`)
- ✅ **name** - имя арендатора (не пусто)
- ✅ **phone** - номер в формате `+7XXXXXXXXXX` (включая +7!)
- ✅ **approved** - должно быть `true` (если false - рентаторе не одобрен)
- ✅ **email** - может быть пусто или содержать email

### Шаг 3: Если номер не найден
Выполните:
```sql
SELECT COUNT(*) as total_records FROM public.tenants;
```

Если таблица пуста или содержит мало записей, добавьте тестового пользователя:
```sql
INSERT INTO public.tenants (name, phone, approved)
VALUES 
  ('Тестовый пользователь', '+79602406033', true);
```

---

## 2. Как включить логирование

### Способ 1: Chrome DevTools (рекомендуется)
1. Откройте сайт: https://paganets.github.io/apraxin/apra-shka-map/
2. Нажмите **F12** (или правый клик → "Проверить элемент")
3. Перейдите на вкладку **Console**
4. Нажмите кнопку **"Вход"** в приложении
5. В консоли появятся логи

### Способ 2: Android Remote Debugging
Для мобильного тестирования:
1. Подключите Android устройство через USB
2. На ПК откройте Chrome
3. Перейдите на `chrome://inspect/#devices`
4. Откройте DevTools удалённого устройства
5. Перейдите на Console

---

## 3. Понимание логов

Вот последовательность логов при успешной аутентификации:

```
🔘 Кнопка Вход нажата                    ← Вы кликнули на кнопку
🔓 Открываю модальное окно входа        ← Модальное окно появилось
📤 Форма входа отправлена               ← Вы нажали "Войти" в форме
📱 Введён номер телефона (сырой): 9602406033   ← Сырой ввод
📝 Длина номера: 10 символов
🔄 Форматирование номера: {input: "9602406033", length: 10}
  └─ Только цифры: 9602406033 (10 цифр)
  └─ Нормализовано: 79602406033 (11 цифр)
  ✅ Результат: +79602406033
✅ Отформатирован номер: +79602406033
✓ Формат: +7 + 10 цифр = 12 символов
🔄 Отправляем запрос в Supabase...
🔍 Auth: Проверка номера телефона +79602406033
✅ Формат номера валиден
📍 URL запроса: https://qagpuucdnkrzwsmnbymk.supabase.co/rest/v1/tenants?phone=eq.%2B79602406033&select=*
📊 Статус ответа: 200
📝 Заголовки ответа: {content-type: "application/json; charset=utf-8", content-range: "0-0/*"}
📦 Полученные данные: [{id: "ec66395d-...", name: "Ale", phone: "+79602406033", ...}]
📊 Количество записей: 1
👤 Найдная запись: {id: "ec66395d-...", name: "Ale", phone: "+79602406033", approved: true, email: "..."}
✅ Auth: Номер верифицирован. Найден арендатор: Ale
✅ Аутентификация успешна, сохраняю сессию
👤 Сессия для: Ale (+79602406033)
🚀 Переходу в админ-панель
```

---

## 4. Частые ошибки и решения

### ❌ Ошибка: "404 (Not Found)" для /index.html

**Причина:** Кеш браузера содержит старый код

**Решение:**
- Нажмите **Ctrl+Shift+R** (полный refresh)
- Или **Cmd+Shift+R** на Mac
- Или очистите кеш: F12 → Сеть → Галочка "Отключить кеш"

---

### ❌ Ошибка: "❌ HTTP ошибка: 401"
```
📊 Статус ответа: 401
❌ HTTP ошибка: {"message":"Invalid API key"}
```

**Причина:** Ключ API неверный или отозван

**Решение:**
1. Откройте Supabase Dashboard → Settings → API
2. Скопируйте актуальный **anon public key**
3. Обновите `meta` тег в `index.html`:
   ```html
   <meta name="supabase-anon-key" content="eyJ...">
   ```
4. Выполните:
   ```bash
   git add . && git commit -m "fix: обновлён ключ Supabase" && git push
   ```

---

### ❌ Ошибка: "⚠️ Номер не найден в базе данных"

**Причина:** Номер не существует в таблице `tenants` или имеет другой формат

**Проверка:**
1. Выполните SQL запрос (см. раздел 1)
2. Убедитесь:
   - ✅ Номер в формате `+7XXXXXXXXXX` (с +7 в начале!)
   - ✅ Записей больше 0
   - ✅ Номер точно совпадает (учитывая пробелы)

**Решение:**
- Добавьте номер в БД через SQL:
  ```sql
  INSERT INTO public.tenants (name, phone, approved)
  VALUES ('Ваше имя', '+79602406033', true);
  ```

---

### ❌ Ошибка: "⚠️ Аккаунт не одобрен"

**Причина:** Поле `approved` = `false` в таблице

**Проверка логов:**
```
👤 Найдная запись: {..., approved: false, ...}
⚠️ Аккаунт не одобрен администратором (approved = false)
```

**Решение:**
1. Откройте Supabase → Table Editor → tenants
2. Найдите запись с вашим номером
3. Установите поле `approved` = `true`
4. Попробуйте снова

---

### ⚠️ Логирование не появляется

**Причина:** JavaScript загружается с кеша

**Решение:**
1. Нажмите **F12** → вкладка **Console**
2. Активируйте галочку **"Preserve log"**
3. Нажмите **Ctrl+Shift+Delete**
4. Очистите **Кеш** и **Cookie**
5. Обновите страницу
6. Попробуйте вход

---

## 5. Тестирование

### Полное тестирование:

1. **Откройте консоль:**
   - F12 → Console → отметьте "Preserve log" ✓

2. **Введите номер:**
   - Без +7: `9602406033`
   - С 8 вместо 7: `89602406033`
   - С кодом: `+79602406033`
   
   (Все варианты должны работать благодаря форматированию)

3. **Смотрите логи:**
   - Вся последовательность должна прямо в консоли

4. **Проверьте результат:**
   - ✅ Если одобрен → переход в админ-панель
   - ❌ Если не одобрен → сообщение об ошибке + лог с "approved: false"
   - 🔴 Если не найден → лог "Номер не найден в базе данных"

---

## 📊 Команды для быстрой проверки

### Проверить есть ли записи в БД:
```sql
SELECT COUNT(*) FROM public.tenants;
SELECT * FROM public.tenants LIMIT 5;
```

### Добавить тестового пользователя:
```sql
INSERT INTO public.tenants (name, phone, approved, email)
VALUES 
  ('Тестовый админ', '+79602406033', true, 'test@example.com');
```

### Очистить все тестовые записи:
```sql
DELETE FROM public.tenants WHERE approved = false;
```

---

## 🆘 Если ничего не помогает

1. **Проверьте консоль на ошибки:**
   - Откройте F12 → Console
   - Ищите красные сообщения об ошибках
   - Скопируйте полный текст ошибки

2. **Проверьте Supabase API Keys:**
   - Settings → API → есть ли "anon" ключ?
   - Ключ не отозван (Revoked = No)?

3. **Проверьте RLS policies:**
   - SQL Editor → выполните:
     ```sql
     SELECT * FROM auth.users LIMIT 1;
     -- Если 401 - проверьте RLS policies на таблице tenants
     ```

4. **Проверьте коннективность:**
   - Откройте DevTools → Network
   - Нажмите кнопку "Вход"
   - Ищите запрос к `supabase.co`
   - Должен быть статус 200, а не 401/403

---

## ✅ Чеклист успешной работы

- [x] В Supabase таблица `public.tenants` содержит запись с номером `+79602406033`
- [x] Поле `approved` имеет значение `true`
- [x] Ключ API скопирован в `meta` тег (первые 20 символов видны в логах)
- [x] При входе видны все логи форматирования и проверки
- [x] HTTP status ответа от Supabase = 200
- [x] Данные о пользователе загружаются и показываются в логах
- [x] После успешного входа переход в админ-панель работает

---

**Если у вас есть вопросы - проверьте логи консоли и сравните с описанием выше!** 🎯
